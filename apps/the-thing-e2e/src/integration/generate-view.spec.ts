describe('Generate view from the-thing model and value', () => {
  it('Should show label of the-thing', () => {
  });

  it('Should show correct value of corresponding type of each property', () => {
  });  
});
