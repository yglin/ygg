describe('Create the-thing model', () => {
  it('Require fields: type, name, label of the-thing', () => {
  });

  it('Require at least one property added', () => {
  });

  it('Check data consistency on submitted', () => {
  });
});
