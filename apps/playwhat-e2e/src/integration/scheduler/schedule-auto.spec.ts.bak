describe('Automatic generated schedule', () => {
  it('every event should take place within date-range of schedule-plan', () => {
  });

  it("every event should take place within its resource's business-hours", () => {
  });

  it('tags of every event should match at least one of those of schedule-plan', () => {
  });
  
});

