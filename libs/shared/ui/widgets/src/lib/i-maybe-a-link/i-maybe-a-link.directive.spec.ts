import { IMaybeALinkDirective } from './i-maybe-a-link.directive';

describe('IMaybeALinkDirective', () => {
  it('should create an instance', () => {
    const directive = new IMaybeALinkDirective();
    expect(directive).toBeTruthy();
  });
});
