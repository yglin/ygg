import { WeekDayPipePipe } from './week-day-pipe.pipe';

describe('WeekDayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekDayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
