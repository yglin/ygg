import { NgForColorBackgroundDirective } from './ng-for-color-background.directive';

describe('NgForColorBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForColorBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
