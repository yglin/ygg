import { RelationDef } from '@ygg/the-thing/core';
import { ImitationProduct } from './product';

export const RelationAddition = new RelationDef({
  name: '加購項目',
  imitationId: ImitationProduct.id
});
