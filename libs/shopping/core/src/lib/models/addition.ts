import { RelationDefine } from '@ygg/the-thing/core';
import { ImitationProduct } from './product';

export const RelationAddition = new RelationDefine({
  name: '加購項目',
  imitationId: ImitationProduct.id
});
